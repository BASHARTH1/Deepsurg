{
  "name": "deepsurg",
  "version": "0.1.0",
  "private": true,
  "description": "DeepSurg — Angular front end and NestJS API",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm --prefix backend run start:dev",
    "dev:web": "npm --prefix frontend start",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test:api": "npm --prefix backend test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
