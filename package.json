{
  "name": "deepsurg",
  "version": "0.1.0",
  "private": true,
  "description": "DeepSurg — Angular marketing site",
  "scripts": {
    "install:all": "npm --prefix frontend install",
    "dev": "npm --prefix frontend start",
    "build": "npm --prefix frontend run build"
  }
}
